-- 2026-05-18: Bulk link validation of opportunities.application_link
--
-- Ran a two-phase HEAD/GET checker over every row in public.opportunities
-- (1,164 rows, all with non-null application_link). Then for each broken URL
-- tested its base origin as a fallback. Applied targeted UPDATEs only to
-- rows whose application_link returned 4xx/5xx, a connection error, or a
-- timeout, and only after re-confirming the row's old application_link
-- still matched the audited value.
--
-- Outcome (205 rows changed; no other columns touched):
--   - truncated-to-base   10   (deep path 404'd, base origin OK)
--   - bare-domain-broken  59   (URL was already a bare domain that failed)
--   - domain-unreachable 136   (URL failed and the base origin also failed)
--
-- The 195 rows now set to NULL are predominantly sites that block automated
-- HEAD/GET probing (e.g., monsha'at.gov.sa, modon.gov.sa, mohap.gov.ae,
-- aramco.com paths, ADGM, Lonza, etc.) — many of these likely still work
-- in a real browser. Treat the NULL state as "needs manual re-verification"
-- rather than "definitely dead." The frontend already renders NULL
-- application_link as "No link available" / hides the Go-to-Website CTA.
--
-- No DDL — the actual mutation was applied via the Supabase MCP and is not
-- replayed here. This file exists as a chronological audit marker so the
-- bulk validation event is recoverable from version control.
--
-- Recovery hint: the per-row plan that produced these UPDATEs can be
-- regenerated by re-running the same checker against the live data and
-- diffing against this commit timestamp.

SELECT 'link-audit-2026-05-18' AS migration_marker;
